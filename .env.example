# Server configuration
PORT=3000
NODE_ENV=development

# Data retention configuration
DB_PATH=./data

# Data retention configuration
DATA_RETENTION_DAYS=14

# Health check interval in milliseconds (default: 1800 seconds / 30 minutes)
CHECK_INTERVAL_MS=1800000